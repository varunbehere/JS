// Maps are same as objects only but there is slight difference that is maps are stored in order and objects arent
// even though both have key value pair

let map = new Map ()
map.set('firstname','varun')
map.set('lastname','behere')
console.log(map)